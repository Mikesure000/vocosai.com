{
  "name": "vocos-backend",
  "version": "0.2.0",
  "description": "Vocos 重构后端 - Node.js HTTP + SQLite 服务",
  "type": "module",
  "main": "src/server.mjs",
  "scripts": {
    "dev": "node --watch src/run-server.mjs",
    "start": "node src/run-server.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "dotenv": "^16.4.7",
    "uuid": "^11.1.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
